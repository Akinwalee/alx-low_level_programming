#include <stdio.h>
#include <string.h>

/**
 * _strlen - Prints the length of a string
 *
 * @s: Pointer variable of the string whose length is to be printed.
 *
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	strlen(*s);
}

#include "main.h"

/**
 * _strcmp - Compares two strings.
 *
 * @s1: First string to compare
 * @s2: Second string to compare with.
 *
 * Return: A value indicating the relationship between the 2 strings.
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
